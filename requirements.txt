# Runtime dependencies for PGVectorRAGIndexer
# For development tools (pytest, black, ruff, mypy), also install requirements-dev.txt

# MCP Server
mcp>=1.0.0

# Core dependencies
psycopg2-binary>=2.9.9
pgvector>=0.2.4
python-dotenv>=1.0.0
watchdog>=3.0.0
xxhash>=3.4.1
lancedb>=0.30.2
pyarrow>=24.0.0
filelock>=3.20.0

# Configuration and validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Document processing
langchain>=0.1.0
langchain-community>=0.0.10
langchain-core>=0.1.0
langchain-text-splitters>=0.0.1
pypdf>=3.17.0
python-docx>=1.1.0
python-pptx>=1.0.0
openpyxl>=3.1.2
pandas>=2.1.0
unstructured>=0.11.0

# OCR support (scanned documents and images)
pytesseract>=0.3.10
pdf2image>=1.16.3
Pillow>=10.0.0

# Embeddings
sentence-transformers>=2.2.2
torch>=2.1.0
numpy>=1.24.0

# API
# Cap below FastAPI 0.120 / Starlette 0.49: Starlette 0.49 wraps include_router()
# entries in an `_IncludedRouter` object inside app.routes, which has no `.path`.
# Our route-registration test helpers iterate `<router>.routes` and read `.path`,
# so unpinned CI runners that pulled Starlette >=0.49 broke ~14 endpoint tests.
# Validated against fastapi 0.119.0 / starlette 0.48.0. Make the helpers
# recurse into _IncludedRouter.routes post-merge, then this cap can be relaxed.
fastapi>=0.108.0,<0.120
uvicorn[standard]>=0.25.0
python-multipart>=0.0.6
starlette>=0.27.0,<0.49

# Backend LanceDB search index
lancedb==0.30.2
pyarrow==24.0.0
filelock>=3.13.0

# Database migrations
alembic>=1.13.0
sqlalchemy>=2.0.0

# License key validation
PyJWT>=2.8.0
cryptography>=41.0.0

# SSO/SAML (enterprise, optional — requires xmlsec1 system package)
python3-saml>=1.16.0

# Version comparison (desktop client)
packaging>=23.0
